app:
  cookie:
    secure: false
    same-site: Lax

frontend:
  url: http://localhost:3000

jwt:
  secret: ${JWT_SECRET}

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop